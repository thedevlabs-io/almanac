node_modules/
dist/
*.vsix

# Agent tooling config & memory — local only
.claude/

# Internal dev-only docs — never commit
docs/PUBLISHING.md

# Local scratch: exported activity data
almanac-export.json
*-export.json

CLAUDE.local.md

# Copied from @fontsource at build time by scripts/build-brand.mjs
media/fonts/

# Rendered panel markup from `npm run preview`: build output, regenerate it
docs/mockups/preview/
